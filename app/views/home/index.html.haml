%h1 This is daily forecast
/= @results

%table.table
  %thead
    %tr
      %th #
      %th Summary
      %th Min Temperature
      %th Max Temperature
      %th Visibility
      %th Wind Speed

  %tbody
    - @results.each_with_index do |result, index|
      %tr
        %th{:scope => "row"}= index+1
        %td= result.summary
        %td= result.apparentTemperatureMin
        %td= result.apparentTemperatureMax
        %td= result.visibility
        %td= result.windSpeed